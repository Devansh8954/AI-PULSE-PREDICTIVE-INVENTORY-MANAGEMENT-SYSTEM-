# Logs directory — created by Winston at runtime
logs/

# Node modules
node_modules/
dist/

# Environment files — NEVER commit real secrets
.env
.env.local
.env.production
.env.staging
# .env.example IS committed intentionally (blank template)

# OS files
.DS_Store
Thumbs.db
